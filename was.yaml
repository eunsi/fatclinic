apiVersion: apps/v1
kind: Deployment
metadata:
  name: fatclinic-was-deployment
  labels:
    app: fatclinic
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fatclinic
  template:
    metadata:
      labels:
        app: fatclinic
        tier: was
    spec:
      containers:
      - name: fatclinic-container
        image: us-west2-docker.pkg.dev/exemplary-datum-362307/quickstart-docker-repo/51c299a9c321:v1
        ports:
        - containerPort: 8080
        resources:
          requests:
            memory: 250Mi
            cpu: 100m
